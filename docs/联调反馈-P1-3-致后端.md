# 联调反馈 · P1-3 节点到货确认（致后端）

| 字段 | 值 |
| --- | --- |
| **阶段** | P1-3 / P1-08 |
| **前端分支** | `frontend/p1-3` |
| **后端分支** | `origin/backend/phase-p1-3`（待联调时填写 commit） |
| **集成分支** | `integration/p1-3`（待创建） |
| **文档日期** | 2026-06-09 |
| **联调状态** | 待联调（Mock 自测已通过） |

---

## 1. 前端交付范围

- 新页 `/arrival-confirm`：方案 + L1 节点筛选、待确认包裹列表、正常/异常确认、级联结果摘要
- API：`GET /simulation/arrival-packages`、`POST /simulation/confirm-arrival`
- Mock：`initMockArrivalDemoState`（GS20260613001 / SO101，C/D/E/F 场景）

---

## 2. 联调前置（§1.4.3 步骤 1～2）

1. Dashboard：preview → confirm 全局方案
2. 节点间调度 L0→L1，使 C/D 为 `in_transit`，E/F 为 `pending_pack`
3. 或确认 `init_demo_data` 可复现 C/D/E/F 演示数据

---

## 3. 验收步骤（§1.4.3 步骤 3～5）

| 步骤 | 操作 | 预期 |
| --- | --- | --- |
| 3 | n_2 到货确认：C=normal，D=exception | E=`packed`，F=`exception` |
| 4 | F005 L1→L2 | 仅 E → `in_transit`；F 保持 `exception` |
| 5 | simulation/deliver | g_1=`delivered`；g_2、F 仍为 `exception` |

---

## 4. 联调问题记录

（联调时填写）

---

## 5. 结论

（联调完成后填写）
